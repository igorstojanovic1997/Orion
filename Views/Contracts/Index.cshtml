@model IEnumerable<Orion.Dtos.ContractIndexDto>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Contracts</h2>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Contract Number</th>
            <th>Username</th>
            <th>Duration(Months)</th>
            <th>Discount Rate</th>
            <th>Gratis Period(Months)</th>
            <th>Status(Active)</th>
            <th>Plan</th>
            <th>Edit</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var contract in Model)
        {
            <tr>
                <td>@contract.ContractId</td>
                <td>@contract.Username</td>
                <td>@contract.Duration</td>
                <td>@contract.DiscountRate %</td>
                <td>@contract.GratisPeriod</td>
                <td>@contract.IsActive</td>
                <td>@contract.PlanNames</td>
                <td>@Html.ActionLink("Edit","Edit", "Contracts", new { id = contract.ContractId }, null)</td>
            </tr>
        }
    </tbody>
</table>
@Html.ActionLink("Add New Contract", "New", null, new { @class = "btn btn-primary" })